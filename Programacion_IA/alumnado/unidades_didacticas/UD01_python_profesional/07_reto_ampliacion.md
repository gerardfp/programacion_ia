# UD01 - Reto de Ampliación: Logging Avanzado y Fixtures en pytest

## Reto
1. Configurar un formateador JSON para el sistema de logging utilizando `python-json-logger`.
2. Crear un `conftest.py` con una fixture `sample_inference_payload` que provea datos de prueba reutilizables para toda la suite de pruebas.
