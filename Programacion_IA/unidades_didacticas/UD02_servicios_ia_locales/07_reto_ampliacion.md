# UD02 - Reto de Ampliación: Streaming de Respuestas en Tiempo Real

## Reto
Implementar una función `generar_stream(prompt: str)` utilizando `httpx.stream()` o generadores en Python que consuma el stream de tokens de Ollama (`"stream": true`) e imprima cada fragmento por pantalla a medida que se genera.
