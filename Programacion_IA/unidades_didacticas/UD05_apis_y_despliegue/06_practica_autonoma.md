# UD05 - Práctica Autónoma: Contenerización Profesional con Dockerfile y `uv`

## Enunciado
El alumnado escribirá un `Dockerfile` optimizado para la API de inferencia creada, utilizando la imagen ligera de `uv` para instalar dependencias y configurando un chequeo de salud (`HEALTHCHECK`).
