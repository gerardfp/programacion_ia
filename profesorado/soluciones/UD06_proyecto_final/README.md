# Proyecto Final Solución: Asistente Documental e Inferencia Privada

Este proyecto demuestra la solución de referencia completa integrando:
- Backend `FastAPI` (`app/`)
- Inferencia Local `Ollama` (`ollama`)
- Almacenamiento Vectorial `ChromaDB`
- Suite de Pruebas Automáticas (`tests/`)

## Despliegue

```bash
docker compose up -d --build
```
